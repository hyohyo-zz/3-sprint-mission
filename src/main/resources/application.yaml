spring:
  application:
    name: discodeit
  servlet:
    multipart:
      maxFileSize: 10MB   #파일 하나의 최대크기
      maxRequestSize: 30MB    #한번에 최대업로드 가능용량


discodeit:
  repository:
    type: file
    file-path: src/main/resources/data/
